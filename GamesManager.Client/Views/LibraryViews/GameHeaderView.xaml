<UserControl x:Class="GamesManager.Client.Views.GameHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GamesManager.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="1000">
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <TextBlock Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                       HorizontalAlignment="Center" TextWrapping="Wrap"
                       Text="{Binding HeaderText}" Foreground="White"/>
            <TextBlock Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                       HorizontalAlignment="Center" TextWrapping="Wrap"
                       Text="{Binding DescriptionText}" Foreground="White"/>
            <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       HorizontalAlignment="Center" TextWrapping="Wrap"
                       Text="0+" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
